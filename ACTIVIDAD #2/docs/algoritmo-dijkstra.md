# Algoritmo de Dijkstra

*(Stub — ownership: Sebastián)*

## Qué resuelve

Camino de menor costo desde un nodo origen hacia los demás (o hacia un destino) en un grafo con pesos **no negativos**.

## Por qué aplica a MediRuta

Los minutos de traslado entre IPS son pesos positivos; buscamos la ruta más rápida.

## Complejidad

O((V + E) log V) con cola de prioridad (a documentar en la implementación).

## Ejemplo con el dataset

*(Completar con un origen/destino del JSON y el camino esperado.)*
